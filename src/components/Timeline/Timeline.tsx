import React from 'react';
import styles from './Timeline.module.css';

const Timeline: React.FC = () => (
  <div className={styles.toolbar} data-testid="Timeline">
  </div>
);

export default Timeline;
