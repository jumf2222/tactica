import React from 'react';
import styles from './Hierarchy.module.css';

const Hierarchy: React.FC = () => (
  <div className={styles.Hierarchy} data-testid="Hierarchy">
    Hierarchy Component
  </div>
);

export default Hierarchy;
